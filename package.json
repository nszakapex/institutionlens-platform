{
  "name": "institutionlens-platform",
  "version": "0.1.0",
  "private": true,
  "description": "InstitutionLens application foundation — synthetic demo only",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@11.6.2",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:security": "vitest run src/lib/security src/lib/env.test.ts src/lib/demo-tenant.test.ts src/lib/health.test.ts src/components/design-system.contract.test.ts src/components/status/evidence-label.test.ts src/authorization src/domain/invariants.test.ts src/repositories/synthetic-organization-repository.test.ts",
    "test:domain": "vitest run src/domain src/authorization src/verticals src/repositories src/application",
    "validate:synthetic": "tsx --require ./test/shims/preload-server-only.cjs scripts/validate-synthetic.ts",
    "scan:secrets": "tsx scripts/scan-secrets.ts",
    "scan:clean-room": "tsx scripts/scan-clean-room.ts",
    "scan:assets": "tsx scripts/scan-assets.ts",
    "verify": "npm run format:check && npm run lint && npm run typecheck && npm run test && npm run validate:synthetic && npm run scan:assets && npm run scan:secrets && npm run scan:clean-room && npm run build"
  },
  "dependencies": {
    "next": "16.2.10",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "server-only": "0.0.1",
    "zod": "4.1.12"
  },
  "devDependencies": {
    "@eslint/eslintrc": "3.3.1",
    "@types/node": "22.18.1",
    "@types/react": "19.1.13",
    "@types/react-dom": "19.1.9",
    "eslint": "9.36.0",
    "eslint-config-next": "16.2.10",
    "prettier": "3.6.2",
    "tsx": "4.20.5",
    "typescript": "5.9.2",
    "vitest": "3.2.4"
  }
}
